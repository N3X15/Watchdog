{% from script_dir + '/_jinja/limetech.macro.yml' import limetech_repo %}
addons:
  connect:
    addon: sourcepawn
    dependencies:
      - sourcemod
    strip-ndirs: 1
    repo: {{ limetech_repo('connect','1.2.0')}}